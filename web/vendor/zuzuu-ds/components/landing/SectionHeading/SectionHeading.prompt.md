SectionHeading from zuzuu-studio. Use via `window.ZuzuuDS.SectionHeading` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface SectionHeadingProps {
index: string; label: string; className?: string;
}
```
