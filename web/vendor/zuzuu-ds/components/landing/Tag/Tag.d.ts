import * as React from 'react';

/**
 * Tag — from zuzuu-studio@0.1.0.
 */
export interface TagProps {
children: React.ReactNode; tone?: "default" | "teal" | "accent"; className?: string;
}

export declare const Tag: React.ComponentType<TagProps>;
