Overline from zuzuu-studio. Use via `window.ZuzuuDS.Overline` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface OverlineProps {
index?: string; children: React.ReactNode; className?: string;
}
```
