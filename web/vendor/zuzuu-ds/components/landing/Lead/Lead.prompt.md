Lead from zuzuu-studio. Use via `window.ZuzuuDS.Lead` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface LeadProps {
size?: "manifesto" | "intro"; className?: string; children: React.ReactNode;
}
```
