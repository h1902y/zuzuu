PointList from zuzuu-studio. Use via `window.ZuzuuDS.PointList` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PointListProps {
points: { label: string; body: React.ReactNode }[];
}
```
