ComparisonTable from zuzuu-studio. Use via `window.ZuzuuDS.ComparisonTable` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ComparisonTableProps {
columns: string[]; rows: { criterion: string; values: (boolean | string)[] }[]; caption?: string;
}
```
