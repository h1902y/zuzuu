import * as React from 'react';

/**
 * Wordmark — from zuzuu-studio@0.1.0.
 */
export interface WordmarkProps {
className?: string; stacked?: boolean;
}

export declare const Wordmark: React.ComponentType<WordmarkProps>;
