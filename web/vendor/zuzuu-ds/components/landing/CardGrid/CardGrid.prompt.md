CardGrid from zuzuu-studio. Use via `window.ZuzuuDS.CardGrid` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface CardGridProps {
columns?: 2 | 3; className?: string; children: React.ReactNode;
}
```
