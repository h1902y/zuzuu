StepCard from zuzuu-studio. Use via `window.ZuzuuDS.StepCard` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface StepCardProps {
step: string; title: string; body: string;
}
```
