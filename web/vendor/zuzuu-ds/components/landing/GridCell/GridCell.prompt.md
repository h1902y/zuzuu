GridCell from zuzuu-studio. Use via `window.ZuzuuDS.GridCell` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface GridCellProps {
children: React.ReactNode; className?: string;
}
```
