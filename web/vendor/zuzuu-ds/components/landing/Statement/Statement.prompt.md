Statement from zuzuu-studio. Use via `window.ZuzuuDS.Statement` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface StatementProps {
index: string; label: string; lead: string; highlight: string;
}
```
