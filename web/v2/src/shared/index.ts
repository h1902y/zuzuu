// src/shared — the wire contract both halves import via `#shared/*`.
//
// Placeholder for Rung 0 (scaffold). Rung 1 replaces this with the real protocol
// absorbed from packages/protocol: opcodes.ts (1-byte WS opcodes), flow.ts
// (FLOW_HIGH_WATER / FLOW_LOW_WATER / ACK_INTERVAL), schemas.ts (REST DTOs), and
// this barrel re-exporting them.
export {};
