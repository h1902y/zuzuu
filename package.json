{
  "name": "webcode-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:daemon dev:web",
    "dev:daemon": "npm run dev -w webcode",
    "dev:web": "npm run dev -w @webcode/web",
    "build": "npm run build -w @webcode/web && npm run build -w webcode",
    "test": "npm run test -w webcode",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3"
  }
}
