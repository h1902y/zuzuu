# evolution/ — "evolve"

What **grows** the agent — the differentiator. `Observability → Evaluation → Generations → Governance` (`README.md` §3③).

- **observability/** — the typed, append-only, tree-shaped **trace**. The keystone — *built first*. ← **first harvest target** (Experiment 1's host-agnostic trace core: `event` / `ids` / `spans` / `otlp`).
- evaluation (score the trace), generations (pin definitions, flip the active pointer), governance (the async human approval inbox) — later.

> Empty — Experiment 1's trace core harvests into `observability/` once concluded. Everything immutable is a *definition* (pinned); everything else is *runtime* captured here as a trace ("pin definitions, observe data").
