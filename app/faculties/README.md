# faculties/ — "be"

What the agent **is**: the four faculties we own and grow (`README.md` §3①, §5).

- **Memory** (episodic) — what happened to *me*; distilled from the trace.
- **Knowledge** (semantic) — what's *true*; substrate ladder `md → relational → graph → vector`.
- **Actions** (procedural) — how to *do* things; the selection-burden ladder + a contents lifecycle.
- **Guardrails** — the membrane; an ordered inspector gate pipeline, pinned in the generation.

> Empty — no experiment has harvested here yet. Cognition / Model / Workspace are **host-owned** and never live here.
