# mns-cli

Alias package for [`motorsandsensors`](https://www.npmjs.com/package/motorsandsensors) — the `mns` CLI (host-agnostic agent faculties + OpenTelemetry session tracing for Claude Code / Codex / Gemini CLI / OpenCode).

```bash
npm i -g motorsandsensors   # ← install this one; it provides the `mns` command
```
